name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Backend tests temporarily disabled due to npm registry issues"

  # test-frontend:
  #   Frontend temporariamente desabilitado devido a problemas com npm registry
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: app/frontend
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: echo "Frontend tests disabled"
